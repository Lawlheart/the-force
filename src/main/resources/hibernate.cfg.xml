<hibernate-configuration>
    <session-factory>
        <!-- Database Connection Settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">"#{ 'jdbc:postgresql://' + @dbUrl.getHost() + ':' + @dbUrl.getPort() + @dbUrl.getPath() }"/></property>

        <!-- SQL Dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQL92Dialect</property>

        <!-- Create the database schema on startup -->
        <property name="hbm2ddl.auto">create</property>

        <!-- Show the queries prepared by hibernate -->
        <property name="show_sql">true</property>

        <!-- Names the annotated entity classes -->
        <mapping class="com.lawlietblack.theforce.model.Device"/>
    </session-factory>
</hibernate-configuration>